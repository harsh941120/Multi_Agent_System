# ===== Core LangChain (v1.x line — required by langchain-tavily) =====
langchain>=1.0.0,<2.0.0
langchain-core>=1.2.11,<2.0.0
# NOTE: langchain-community has been sunset by the LangChain team as part
# of the 1.0 migration and is intentionally NOT included here.

# ===== Mistral LLM integration (replaces OpenAI) =====
langchain-mistralai>=1.1.5,<2.0.0
mistralai>=1.2.3

# ===== Groq LLM integration (higher free-tier rate limits than Mistral) =====
langchain-groq>=1.1.3,<2.0.0
groq>=0.11.0

# ===== Tools =====
# Tavily search tool (this is what you likely mean by "Tabli")
langchain-tavily>=0.2.18,<0.3.0
tavily-python>=0.5.0

# BeautifulSoup web scraping tool
beautifulsoup4>=4.12.3
requests>=2.32.3
lxml>=5.3.0

# ===== Agents (ReAct) + LCEL / Runnables support =====
# ReAct agent building blocks and Runnable pipelines are part of
# langchain / langchain-core above (create_react_agent, AgentExecutor,
# RunnableSequence, RunnableParallel, RunnablePassthrough, etc.)

# ===== Env / config =====
python-dotenv>=1.0.1

# ===== Optional but commonly needed in multi-agent research setups =====
pydantic>=2.9.2
tenacity>=9.0.0

# ===== Data handling =====
pandas>=2.0.0

# ===== JSON handling improvements =====
orjson>=3.12.0

# ===== Optional (for advanced parsing / HTML cleanup) =====
html5lib>=1.1

# ===== Logging / debugging (optional but recommended) =====
rich>=15.0.0

# ===== Async support (important for agents/tools) =====
aiohttp>=3.14.3

streamlit>=1.61.0